meta {
  name: Update Task
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/tasks/{{taskId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "in_progress",
    "priority": "medium"
  }
}
