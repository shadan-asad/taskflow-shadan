meta {
  name: Update Project
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/projects/{{projectId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Updated Project Name"
  }
}
